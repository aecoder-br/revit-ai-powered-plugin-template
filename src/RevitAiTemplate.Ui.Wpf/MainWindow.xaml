<Window x:Class="RevitAiTemplate.Ui.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit AI Assistant" Height="560" Width="760"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Text="AI-powered Revit assistant" FontSize="20" FontWeight="SemiBold" />

    <TextBox Grid.Row="1" Margin="0,12,0,12" MinHeight="70" TextWrapping="Wrap"
             Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}" />

    <TextBox Grid.Row="2" Text="{Binding Result}" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
             IsReadOnly="True" AcceptsReturn="True" />

    <StackPanel Grid.Row="3" Margin="0,12,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
      <TextBlock VerticalAlignment="Center" Margin="0,0,12,0" Text="{Binding Status}" />
      <Button Width="140" Height="32" Content="Analyze model" Command="{Binding AnalyzeCommand}" />
    </StackPanel>
  </Grid>
</Window>
